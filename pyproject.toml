[project]
name = "backtalk"
version = "1.0.0"
description = "Talk to your Claude Code agent out loud. Hold a key, speak, and it answers in a real voice."
requires-python = ">=3.11,<3.13"
dependencies = [
    "claude-agent-sdk>=0.2.100",
    "faster-whisper>=1.0.0",
    "httpx>=0.27.0",
    "kokoro>=0.9.0",
    # Apple Silicon only: runs the SAME whisper model on the GPU.
    # CTranslate2 (under faster-whisper) has no Metal backend, so
    # without this every Mac transcribes on the CPU. Measured 0.88s
    # -> 0.12s on an M4 Max with an identical transcript. Every other
    # platform keeps faster-whisper, which already uses CUDA.
    "mlx-whisper>=0.4.0 ; sys_platform == 'darwin' and platform_machine == 'arm64'",
    "numpy>=1.26.0",
    "pynput>=1.8.0",
    "setuptools<81",
    "sounddevice>=0.5.0",
    "soundfile>=0.12.0",
    # ibuy-custom: webrtcvad's sdist needs an MSVC C compiler to build on
    # Windows (no wheels published upstream). webrtcvad-wheels is a
    # community fork that ships prebuilt Windows wheels and installs as
    # the same importable `webrtcvad` module -- confirmed identical API
    # by testing `webrtcvad.Vad(2)` directly. Swapped here (not just
    # installed ad hoc) so `uv sync` resolves it instead of trying to
    # rebuild the broken original from source on every run.
    "webrtcvad-wheels>=2.0.10",
]

[tool.setuptools]
packages = ["backtalk"]

[tool.uv]
package = false
